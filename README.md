# Handwritten-Digit-Classifier-Using-SVM

This is a Machine learning project which Contains a Support vector Classifer Model trained on MINIST grayscale dataset and i have used Flask framework to make a web Application and it is deployed on render.
